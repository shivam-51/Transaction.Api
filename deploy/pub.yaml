apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: servicebus-pubsub
spec:
  type: pubsub.azure.servicebus
  version: v1
  metadata:
  - name: connectionString # Required when not using Azure Authentication.
    value: "Endpoint=sb://{ServiceBusNamespace}.servicebus.windows.net/;SharedAccessKeyName=sharedaccesskey;SharedAccessKey=ziV5yR3XKDbbbIbWmN5MJIKWBdtRpVOhu+ASbJlh51I=;EntityPath={ServiceBus}"
  - name: topicName # Required when not using Azure Authentication.
    value: "dapr-topic"
